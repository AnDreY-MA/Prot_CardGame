using UnityEngine;

[ExecuteInEditMode]
public class SpaceEditor : MonoBehaviour
{
    /*private WayPoint _wayPoints;

    private void Start()
    {
        _wayPoints = GetComponent<WayPoint>();
    }

    private void Update()
    {
        transform.position = new Vector2(_wayPoints.GridPos().x * _wayPoints.GridSizeX, _wayPoints.GridPos().y * _wayPoints.GridSizeY);
    }*/
}
